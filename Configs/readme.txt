This is where configuration files will be stored that the other files will refer to when operating.

Train and Test CFG are configuration files that allow access to the larcv threadio for the training and testing data of PSingle.

The config_main houses the basic data used in running the network. For editing multiple runs it's easy to simply change this file around.
