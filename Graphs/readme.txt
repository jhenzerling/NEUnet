This is where Tensorboard graphs are stored.

Each file is 3.0 Gb and allows access to the TensorBoard suites. Be careful about memory since these files really add up.
The triggers in 'main.py' allow the choice for whether to create a graph or not.
